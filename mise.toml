# pnpm は `pnpm` ではなく `"npm:pnpm"` を指定する。
# 前者は GitHub API に依存しており OAuth トークン (gh CLI のログイン情報など) が無いと
# 60 req/h の匿名アクセス上限でトラブルになる事例が報告されている。また pnpm 自身が
# Node.js をバンドルするため Node.js が二重にダウンロードされる。
# npm 経由で取得することでいずれの問題も回避できる。
[tools]
node = "22.22.3"
"npm:pnpm" = "11.2.2"
